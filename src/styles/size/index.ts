import { normal } from '@/src/styles/size/size.normal';
import { percent } from '@/src/styles/size/size.percent';
import {
  dvh, dvw, vh, vw
} from '@/src/styles/size/size.width-height';
import { text } from './size.text';

export const size = {
  normal,
  percent,
  vw,
  vh,
  dvw,
  dvh,
  text,
};
