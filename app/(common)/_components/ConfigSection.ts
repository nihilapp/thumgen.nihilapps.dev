'use client';

import styled from 'styled-components';
import { size } from '@/src/styles';

export const ConfigSection = styled.div`
  border: 1px solid #e0e0e0;
  border-radius: ${size.normal[2]};
  padding: ${size.normal[4]};
  margin-bottom: ${size.normal[4]};
`;
